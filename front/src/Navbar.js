import React from "react";

const Navbar = () => {
  return (
    <nav>
      <div className="Navbar">
        <h1>Prueba</h1>
        <ul>
          <button>Login</button>
          &nbsp;&nbsp;
          <button>Logout</button>
        </ul>
      </div>
    </nav>
  );
};

export default Navbar;
